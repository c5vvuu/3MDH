# 📦 دليل النسخ الاحتياطي واستعادة متجر Digitex

### أولاً: أخذ نسخة احتياطية من قاعدة بيانات MySQL
1. ادخل إلى **cPanel**.
2. افتح أداة **phpMyAdmin**.
3. انقر على اسم قاعدة البيانات الخاصة بالمتجر من العمود الجانبي الأيسر.
4. انقر على تبويب **Export** (تصدير) في الشريط العلوي.
5. اختر صيغة **SQL** واضغط على الزر **Export** (أو Go).
6. سيتم تنزيل ملف بامتداد `.sql` يحتوي على كافة الجداول والمنتجات والطلبات والعملاء.

---

### ثانياً: أخذ نسخة احتياطية من ملفات المتجر والصور
1. ادخل إلى **File Manager** في cPanel.
2. انتقل إلى مجلد المتجر (مثل `public_html`).
3. حدد جميع الملفات والمجلدات.
4. انقر بالزر الأيمن أو من القائمة العلوية على **Compress** (ضغط).
5. اختر نوع الأرشيف **Zip Archive** واضغط **Compress Files**.
6. بعد اكتمال الضغط، حدد الملف المضغوط واضغط **Download**.

---

### ثالثاً: استعادة النسخة الاحتياطية (Restore)
1. **استعادة الملفات**: ارفع ملف الـ `.zip` عبر File Manager واستخرج محتوياته (Extract).
2. **استعادة قاعدة البيانات**:
   - أنشئ قاعدة بيانات جديدة أو أفرغ القديمة من phpMyAdmin.
   - اضغط على **Import** واجلب ملف الـ `.sql`.
   - عدّل بيانات الاتصال في ملف `.env` لترتبط بالقاعدة الجديدة.
