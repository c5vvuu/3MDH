# 🚀 دليل النشر والتشغيل الشامل لمتجر عمده ستور (3mdh store)
## رابط المتجر الرسمي: https://3mdh.store/

تهانينا! متجرك **عمده ستور (3mdh store)** جاهز 100% للنشر والتشغيل على أي استضافة ترغب بها (سواء استضافة cPanel عادية، استضافة VPS، أو منصات السحاب مثل Vercel / Render / Cloud Run / Namecheap / Hostinger).

---

### الفهرس
1. [كيفية تحميل ملفات المشروع من AI Studio](#1-كيفية-تحميل-ملفات-المشروع-من-ai-studio)
2. [الخيار الأول: النشر السريع والمباشر (Node.js / VPS / Cloud Hosting)](#2-الخيار-الأول-النشر-السريع-والمباشر-nodejs--vps--cloud)
3. [الخيار الثاني: النشر على استضافة cPanel (Namecheap / Hostinger)](#3-الخيار-الثاني-النشر-على-استضافة-cpanel)
4. [إنشاء قاعدة بيانات MySQL واستيراد schema.sql](#4-إنشاء-قاعدة-بيانات-mysql-واستيراد-schemasql)
5. [ربط الدومين الرسمي https://3mdh.store/ وشهادة SSL المجانية](#5-ربط-الدومين-الرسمي-https3mdhstore-وشهادة-ssl)
6. [لوحة تحكم الإدارة Admin Panel وإدارة طرق الدفع اليدوية](#6-لوحة-تحكم-الإدارة-admin-panel-وإدارة-طرق-الدفع-اليدوية)
7. [إعداد البريد الإلكتروني SMTP للفواتير](#7-إعداد-البريد-الإلكتروني-smtp-للفواتير)
8. [تصدير وتحميل تطبيق الأندرويد (APK)](#8-تصدير-وتحميل-تطبيق-الأندرويد-apk)

---

### 1. كيفية تحميل ملفات المشروع من AI Studio
يمكنك تنزيل ملفات المشروع بالكامل إلى جهاز الكمبيوتر الخاص بك (Windows):
1. في واجهة **Google AI Studio**، اضغط على أيقونة الإعدادات أو زر القائمة الثلاث نقاط **⋮** في الزاوية العلوية.
2. اختر **Export to ZIP** (أو **Export to GitHub** إذا كنت تفضل رفعه إلى حسابك في GitHub).
3. سيبدأ تحميل ملف مضغوط بصيغة `.zip` يحتوي على كامل الكود المصدري للمتجر، مع ملفات البناء وقاعدة البيانات `database/schema.sql`.
4. فك الضغط عن الملف في جهازك Windows.

---

### 2. الخيار الأول: النشر السريع والمباشر (Node.js / VPS / Cloud)
المشروع مبني بـ **React + Vite + Node.js/Express**:
1. افتح موجه الأوامر (CMD أو PowerShell) داخل مجلد المشروع.
2. قم بتثبيت الحزم وبناء المشروع عبر الأمرين:
   ```bash
   npm install
   npm run build
   ```
3. لتشغيل المتجر على الخادم أو جهازك:
   ```bash
   npm start
   ```
   سيعمل المتجر على المنفذ `3000`، ويمكنك توجيه Nginx أو الدومين `https://3mdh.store/` إليه عبر Reverse Proxy.

---

### 3. الخيار الثاني: النشر على استضافة cPanel
إذا كانت استضافتك تعمل بنظام cPanel (مثل Namecheap أو Hostinger):
1. **أداة Node.js في cPanel (Setup Node.js App)**:
   - افتح **cPanel** ← ابحث عن **Setup Node.js App**.
   - اضغط على **Create Application**.
   - اختر إصدار Node.js (مثل **18.x** أو **20.x**).
   - Application root: مجلد موقعك (مثلاً `public_html`).
   - Application URL: دومين متجرك `https://3mdh.store/`.
   - Application startup file: `dist/server.cjs` (أو `server.ts`).
   - اضغط **Create** ثم **Run NPM Install**.
2. **رفع الملفات عبر File Manager**:
   - افتح **File Manager** في cPanel.
   - ادخل إلى `public_html`.
   - ارفع محتويات المشروع (بما في ذلك مجلد `dist/` ومجلد `database/` وملف `package.json`).

---

### 4. إنشاء قاعدة بيانات MySQL واستيراد schema.sql
1. في cPanel، افتح **MySQL® Database Wizard**.
2. أنشئ قاعدة بيانات باسم (مثلاً: `amdh_store`).
3. أنشئ مستخدم للقاعدة بكلمة مرور قوية واختر **All Privileges** (كافة الصلاحيات).
4. افتح **phpMyAdmin**:
   - اختر قاعدة البيانات التي أنشأتها من القائمة الجانبية.
   - اضغط على تبويب **Import** (استيراد).
   - اختر ملف `database/schema.sql` من مجلد المشروع.
   - اضغط **Go**. سيتم استيراد كافة الجداول الـ 25+ فورياً بكامل الإعدادات والبيانات الأولية.

---

### 5. ربط الدومين الرسمي https://3mdh.store/ وشهادة SSL
1. تأكد من توجيه سجلات A Record أو Nameservers لدومينك `3mdh.store` نحو عنوان IP خادمك/استضافتك.
2. من cPanel، ابحث عن **SSL/TLS Status** أو **Let's Encrypt SSL**.
3. حدد دومين `3mdh.store` واضغط **Run AutoSSL** لتفعيل التشفير الآمن مجاناً `https://`.

---

### 6. لوحة تحكم الإدارة Admin Panel وإدارة طرق الدفع اليدوية
- **الدخول للوحة التحكم**:
  - اضغط على زر **لوحة الإدارة** في أسفل المتجر أو توجه مباشرة للمسار:
    ```
    https://3mdh.store/
    ```
    (ثم افتح لوحة المدير عبر نافذة الدخول: `admin@3mdh.store` أو البريد الافتراضي).
- **إدارة طرق الدفع**:
  - انتقل إلى تبويب **الإعدادات وبوابات الدفع** (Settings).
  - ستجد **بوابة PayPal** مفعّلة حصرياً مع خيار إدخال الـ Client ID ووضع Live/Sandbox.
  - قسم **طرق الدفع اليدوية**:
    - اضغط **إضافة طريقة دفع جديدة** لإضافة محافظ الأردن (زين كاش، أورنج موني، CliQ) أو المحافظ العالمية (Binance Pay، RedotPay، STC Pay).
    - حدد العملة الخاصة بكل طريقة (JOD, USDT, USD, SAR, AED, EGP).
    - أدخل رقم الحساب أو معرّف Binance ID واسم المستلم وتعليمات التحويل.
- **معالجة طلبات الدفع اليدوي**:
  - من تبويب **الطلبات (Orders)**، ستظهر الطلبات اليدوية بحالة `pending_payment`.
  - اضغط على الطلب لمعاينة رقم الحوالة واسم المرسل ورابط إشعار الدفع، ثم انقر **مكتمل** للتسليم.

---

### 7. إعداد البريد الإلكتروني SMTP للفواتير
1. في cPanel، افتح **Email Accounts** وأنشئ بريداً رسمياً (مثل `noreply@3mdh.store` أو `support@3mdh.store`).
2. في لوحة تحكم المتجر Admin Panel ← انتقل إلى **Settings** ← **إعدادات البريد SMTP**:
   - **SMTP Host**: `mail.3mdh.store`
   - **Port**: `465` (SSL)
   - **Username**: `noreply@3mdh.store`
   - **Password**: كلمة مرور البريد
3. اضغط **حفظ** ليبدأ المتجر بإرسال الفواتير وأكواد التفعيل للعملاء تلقائياً.

---

### 8. تصدير وتحميل تطبيق الأندرويد (APK)
المشروع جاهز بالكامل لإنتاج تطبيق APK وتحديثه من لوحة التحكم في تبويب **تطبيق الأندرويد (APK Hub)**:
- يمكنك إنشاء الحزمة بضغطة واحدة باستخدام Capacitor:
  ```bash
  npx @capacitor/cli init "3mdh Store" store.app.amdh
  npx @capacitor/cli add android
  npm run build
  npx @capacitor/cli copy android
  npx @capacitor/cli open android
  ```
- من Android Studio اختر **Build > Build Bundle(s) / APK(s) > Build APK(s)**، وانسخ ملف الـ APK إلى استضافتك ليتمكن عملاء المتجر من تحميله مباشرة!
